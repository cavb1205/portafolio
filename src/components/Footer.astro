---
const currentYear = new Date().getFullYear();

const socialLinks = [
  {
    name: "GitHub",
    url: "https://github.com/camilovaron",
    icon: "ri-github-fill"
  },
  {
    name: "LinkedIn",
    url: "https://www.linkedin.com/in/camilovaron/",
    icon: "ri-linkedin-box-fill"
  },
  {
    name: "Email",
    url: "mailto:ing.camilo.varon@gmail.com",
    icon: "ri-mail-fill"
  }
];
---

<footer class="bg-dark-darker py-8">
  <div class="container mx-auto px-4">
    <div class="flex flex-col md:flex-row justify-between items-center gap-6">
      <!-- Logo y derechos -->
      <div class="flex flex-col items-center md:items-start">
        <div class="flex items-center gap-2 mb-3">
          <span class="text-primary font-bold text-xl">CV</span>
          <span class="text-white">Camilo Varón</span>
        </div>
        <p class="text-gray-400 text-sm text-center md:text-left">
          © {currentYear} Camilo Varón. Todos los derechos reservados.
        </p>
      </div>

      <!-- Redes sociales -->
      <div class="flex gap-4">
        {socialLinks.map((link) => (
          <a 
            href={link.url} 
            target="_blank" 
            rel="noopener noreferrer" 
            class="flex items-center justify-center w-10 h-10 bg-dark-lighter rounded-full text-gray-300 hover:text-primary hover:bg-opacity-80 transition-colors"
            aria-label={link.name}
          >
            <i class={`${link.icon} text-xl`}></i>
          </a>
        ))}
      </div>
    </div>
    
    <!-- Mensaje adicional -->
    <div class="mt-6 text-center">
      <p class="text-gray-500 text-xs">
        Desarrollado con <i class="ri-heart-fill text-red-500"></i> usando Astro y Tailwind CSS
      </p>
    </div>
  </div>
</footer> 